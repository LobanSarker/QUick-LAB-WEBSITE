name = "quick-lab-website"
compatibility_date = "2026-08-02"

[assets]
directory = "./out"

# Uncomment and set to wire up the newsletter form (see
# components/NewsletterForm.tsx). Prefer the Cloudflare dashboard
# (Workers & Pages → quick-lab-website → Settings → Variables) for
# secrets so they don't land in git.
# [vars]
# NEXT_PUBLIC_NEWSLETTER_ENDPOINT = "https://example.com/api/subscribe"